export enum CheckResultEnum{
    pass,
    repeat,
    refer,
    error
}

export enum CheckSpO2Enum{
    fail,
    inconclusive,
    pass,
    error
}

export enum CheckSp02DifferenceEnum{
    pass,
    fail,
    error
}